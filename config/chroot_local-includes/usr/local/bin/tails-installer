#!/bin/sh

# Same purpose as the tails-persistent-storage script, see the comment there.

user_env=$(mktemp --suffix -env)
env --null > "${user_env}"
sudo -u tails-persistent-storage /usr/local/lib/connect-drop-tails-installer "${user_env}"
